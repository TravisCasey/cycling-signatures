cycling-signatures gallery
==========================

Worked examples that query the Python bindings to reproduce the Lorenz figures.
Each example loads the committed fixtures and renders one figure type, with the
binding queries shown inline.

.. toctree::
   :maxdepth: 2

   auto_examples/index
